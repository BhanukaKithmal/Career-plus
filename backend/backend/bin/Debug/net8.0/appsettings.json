{
  "Logging": {
    "LogLevel": {
      // Sets the default logging level to "Information"
      "Default": "Information",

      // Logs only warnings and higher severity messages from ASP.NET Core
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Allows requests from any host (* means no restrictions)
  "AllowedHosts": "*",

  "ConnectionStrings": {
    // Database connection string for local SQL Server
    "local": "Server=.;Database=ResumeManagementDRTS;Trusted_Connection=true;TrustServerCertificate=true;"
  }
}
